3. Изменение имени пользователя

###ПОЗИТИВНЫЙ КЕЙС: успешное изменение имени пользователя

Предусловия (делаю через API):
1) Администратор создает пользователя с ролью - USER
2) Пользователь логинится
2.1) Сохраняем полученный токен в localStorage браузера

Шаги теста(UI):
1) Открыть страницу дашборда: Selenide.open("/dashboard").
2) Убедиться, что открыта панель "User Dashboard" (виден заголовок или приветствие).
3) В хедере страницы (где отображаются @kate1998 и "Noname") нажать на имя "Noname" (или на username).
4) Дождаться открытия страницы "Edit Profile".
5) На странице "Edit Profile" в поле "Enter new name" ввести валидное имя, состоящее из двух слов только из букв (вызов соответствующего метода из RandomData).
6) Нажать кнопку "Save Changes".
7) (UI) Отображается сообщение об успехе: "✅ Name updated successfully!"
8) Нажать кнопку "Home", перезагрузить страницу

Ожидаемый результат / Асссерты:
1) (UI) В хедере страницы User Dashboard имя Noname изменилось на новое имя
2) (UI) В приветствии "Welcome,<>!" имя также обновлено: "Welcome, <новое имя>"
3) (API) GET-запросом к API (/customer/profile) убедиться, что в профиле пользователя поле name равно новому имени.

###НЕГАТИВНЫЙ КЕЙС: некорректное имя пользователя при редактировании профиля

Предусловия (делаю через API):
1) Администратор создает пользователя с ролью - USER
2) Пользователь логинится
2.1) Сохраняем полученный токен в localStorage браузера

Шаги теста(UI):
1) Открыть страницу дашборда: Selenide.open("/dashboard").
2) Убедиться, что открыта панель "User Dashboard" (виден заголовок или приветствие).
3) В хедере страницы (где отображаются @kate1998 и "Noname") нажать на имя "Noname" (или на username).
4) Дождаться открытия страницы "Edit Profile".
5) На странице "Edit Profile" в поле "Enter new name" ввести НЕвалидное имя (вызвать метод из RandomData)
6) Нажать кнопку "Save Changes".
7) (UI) Отображается сообщение об ошибке: "Name must contain two words with letters only"
8) Нажать кнопку "Home", перезагрузить страницу

Ожидаемый результат / Асссерты:
1) (UI) Имя в хедере и в приветствии не изменилось (осталось noname)
2) (UI) В приветствии "Welcome, noname!" имя осталось дефолтным.
3) (API) GET-запросом к API (/customer/profile) убедиться, что в профиле пользователя поле name осталось дефолтным (NULL)